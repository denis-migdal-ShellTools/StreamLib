#!/usr/bin/bash

function read_args() {
	declare VARNAME="$1"
	shift

	# si stdin est le terminal, permettre l'édition de la ligne.
	declare EDITOR=""
	if [[ -t 0 ]] ; then
		EDITOR="-e"
	fi

	read $EDITOR -a "$VARNAME" "$@"
}
